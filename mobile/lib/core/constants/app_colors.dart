import 'package:flutter/material.dart';

class AppColors {
  // Brand Primary & Accents
  static const Color primary = Color(0xFF0F172A);      // Slate 900
  static const Color secondary = Color(0xFF1E293B);    // Slate 800
  static const Color accent = Color(0xFF06B6D4);       // Cyan 500
  static const Color accentLight = Color(0xFF67E8F9);  // Cyan 300
  static const Color indigo = Color(0xFF6366F1);       // Indigo 500

  // Background & Surface
  static const Color background = Color(0xFF0B0F19);   // Deep Navy/Dark
  static const Color surface = Color(0xFF161E2E);      // Surface Card Dark
  static const Color surfaceLight = Color(0xFF222F43);

  // Status & Progress
  static const Color success = Color(0xFF10B981);      // Emerald
  static const Color warning = Color(0xFFF59E0B);      // Amber
  static const Color error = Color(0xFFEF4444);        // Rose/Red
  static const Color info = Color(0xFF3B82F6);         // Blue

  // Text & Neutral
  static const Color textPrimary = Color(0xFFF8FAFC);
  static const Color textSecondary = Color(0xFF94A3B8);
  static const Color textMuted = Color(0xFF64748B);
  static const Color border = Color(0xFF334155);
}
